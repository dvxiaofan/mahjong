# 补充建议（GLM）

- **日期**：2026-09-04
- **性质**：对《游戏需求文档》与当前实现的补充建议及收尾记录。与规则冲突时，一律以上级目录《自定义麻将规则.md》为准
- **背景**：本文档记录 M1 收尾结果；当前源码含 28 个测试用例，规则文档含 2026-09-04 全部澄清修订

---

## 一、引擎与规则文档的对齐情况

**争抢优先级已对齐**：

- 规则文档第三章/第五章：「打出这张牌的**下家**优先，沿逆时针依次判定」
- 引擎实现：`claimOrder`（`src/game.ts:62`）使用 `seatOrderFrom(nextSeat(discarder))`
- 当前行为与定稿规则“弃牌者下家优先”一致；收尾测试已使用非庄家弃牌者验证该顺序

## 二、M1 收尾测试覆盖

收尾前的测试覆盖了流程与判定，但结算金额类断言不足；本轮已补充以下场景：

1. 自摸胡：三家各付 `1 + 番数`（含番种明细验证）
2. 点炮胡：仅点炮者付 `1 + 番数`
3. 暗杠：终局三家各付 1；明杠：终局点杠者付 1
4. 补杠：即时付 1，杠上自摸后保留该分
5. 0 财不能点炮胡（`fortuneCount < 1` 分支）
6. 同一弃牌按弃牌者下家顺序竞争，且覆盖非庄家弃牌者

## 三、规则边界约定（已纳入定稿）

1. 以上两项边界已按当前引擎行为补入定稿规则：前位 `pass` 后继续看下一位；响应窗口内可先报嘴再选择胡或过

## 四、联机版设计缺口（M4 前置，当前文档只有路线图）

《游戏需求文档》2.3 节列了功能清单但没有设计说明。真正要做的新东西按优先级：

| # | 事项 | 说明 | 优先级 |
|---|---|---|---|
| 1 | **状态投影** `projectStateForSeat(state, seat)` | 当前 `GameState` 全透明，联机上直接广播 = 对家手牌泄露。只暴露自己暗牌 + 公共信息。**必须在 UI 之前设计**，因为 UI 渲染依赖投影后的视图 | 最高 |
| 2 | **多局会话层** | 引擎只管单局。需 Match/Session 模块：上局赢家做庄、跨局累计分、一场 N 局、平局处理 | 高 |
| 3 | **服务端与部署选型** | 引擎纯函数可直接跑服务端。建议评估 Cloudflare Durable Objects（WebSocket + 有状态房间，一个 DO 实例 = 一张牌桌，纯 JS 引擎可直接入 Workers） | 高 |
| 4 | **断线重连** | 引擎快照 + 事件日志已支持回放，只差协议层封装 | 中 |
| 5 | **超时托管** | claim 窗口超时默认注入 pass（`allResponsesReceived` 同步模型已为此铺路）；出牌超时的策略待定 | 中 |

并发问题已被「等齐三家响应才决胜」的同步模型消掉，不需要复杂并发设计——这一点比《游戏需求文档》60 行的预判乐观。

## 五、《游戏需求文档》已过时的内容（供对照）

- **3.4 报嘴节**：「不报嘴不能胡，包括自摸」——已被新规则取代（≥3 财未报嘴可自摸、不可点炮）
- **第 9 节 12 条待确认事项**：已由规则文档 2026-09-04 修订及本次边界约定基本覆盖；骰子映射和多局会话仍属于后续产品设计
- **4.4 动作类型**：`WIN_SELF_DRAW / WIN_DISCARD` 分离设计，实际引擎用单一 `win` 动作 + 阶段上下文，等价
- **5 状态机 10 状态**：实际引擎收敛为 5 个 phase（awaiting-draw / awaiting-discard / claiming / finished / drawn），足够
- **4.1 React + Vite**：M2.3 已加入 React/Vite UI 壳；动作接入和完整本地对战仍待 M2.4 之后完成

## 六、后续处理顺序

1. 新环境执行 `npm ci`（本次收尾已跑通 `check`、`test`、`build`）
2. M2：状态投影 + 单机 UI（投影先行，联机直接复用）
3. M3：AI
4. M4：按第四节清单做联机
