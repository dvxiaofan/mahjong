# 开发实施计划与进度台账

> 这是一个随项目更新的实施文档，用来让新加入的开发者或后续 AI 在进入项目时快速了解：项目当前处于哪一阶段、每一步要做什么、完成标准是什么，以及下一步从哪里开始。

- **文档类型**：持续维护的开发计划（不是规则原文）
- **最后更新**：2026-09-05
- **当前阶段**：M0-M5 全部已完成，v1.0.0 已具备本地发布条件
- **最近提交**：`631b009 release: prepare v1.0.0 deployment`
- **当前验证基线**：干净安装与 `npm run quality` 全部通过；127 个测试；总体行覆盖率 83.90%；生产依赖审计 0 漏洞

## 一、阅读和维护规则

### 1.1 状态标记

- `[x]` 已完成：实现、测试和验收均已完成
- `[~]` 进行中：当前正在实施；同一时间原则上只保留一个主步骤为进行中
- `[ ]` 待开始：尚未实施
- `[?]` 待确认：需要产品/规则决定后才能冻结
- `[!]` 阻塞：已发现阻塞原因，需外部条件或明确决策

### 1.2 每一步的完成条件

一个步骤只有同时满足以下条件才可以标记为 `[x]`：

1. 代码或文档产物已经落盘；
2. 相关测试或人工验收已经完成；
3. 没有遗留的明显 TODO 影响该步骤目标；
4. 在本台账的“进度日志”中记录结果和 commit；
5. 创建一个本地 Git commit。

### 1.3 Git 约定

- 每完成一个明确步骤就创建一个本地 commit；
- 不自动执行 `git push`；
- commit 前先运行与步骤匹配的检查，并确认 `git diff --check` 通过；
- 不把 `node_modules/`、`dist/` 或本地临时文件提交进仓库；
- 若工作区已有用户修改，先区分归属，不覆盖或混入无关改动。

## 二、项目边界和依据

### 2.1 规则依据优先级

1. [自定义麻将规则.md](../自定义麻将规则.md)：唯一的规则最高依据；
2. [游戏需求文档.md](./游戏需求文档.md)：产品和技术需求；
3. [补充建议.md](./补充建议.md)：审阅意见和收尾记录；
4. 本文档：实施顺序、验收标准和进度记录。

如果分析文档和规则定稿冲突，以规则定稿为准；如果产品需求和当前代码冲突，先记录决策，再修改代码和测试。

### 2.2 当前实现边界

当前仓库是纯 TypeScript 单局规则引擎，核心入口为：

- `createGame()`：创建牌墙并发牌；
- `getLegalActions()`：根据状态生成合法动作；
- `applyAction()`：校验动作并推进状态；
- `getCurrentWaitingTiles()` / `getCurrentFanIfWinning()`：提供 UI 可用的查询。

当前尚未包含：浏览器 UI、正式 AI、多局会话、联网协议、用户账号和部署配置。

## 三、总路线图

| 里程碑 | 目标 | 状态 | 进入条件 | 退出标准 |
|---|---|---:|---|---|
| M0 | 规则定稿和工程脚手架 | [x] | 已有规则文本和仓库 | 规则边界可供编码 |
| M1 | 单局 TypeScript 规则引擎 | [x] | M0 完成 | 单局可推进、结算可验证、测试通过 |
| M2 | 本地可玩的牌桌 UI | [x] | M1 完成；先做视图契约 | 1 人操作、3 个占位/基础 AI 可完整打一局 |
| M3 | 可解释的 AI 对手 | [x] | M2 有稳定动作适配层 | AI 不执行非法动作，策略和难度可测试 |
| M4 | 多局和联网对战 | [x] | M2/M3 基础稳定，协议和隐私设计完成 | 服务端权威、可重连、可完整对局 |
| M5 | 发布质量和运营准备 | [x] | M4 或明确只发布单机版 | E2E、性能、兼容性和发布文档完成 |

## 四、M0：规则与工程基线（已完成）

### 0.1 规则定稿 `[x]`

- 明确 120 张牌、发财补牌、禁吃和三家通碰；
- 明确暗杠、明杠、补杠及无抢杠胡；
- 明确标准牌型、碰碰胡、清一色和发财番；
- 明确报嘴锁定、点炮限制和荒庄杠分处理；
- 明确同一弃牌按弃牌者下家开始、沿逆时针竞争。

**产物**：[自定义麻将规则.md](../自定义麻将规则.md)

### 0.2 需求和路线图 `[x]`

- 确定先做单局引擎，再做本地 UI、AI、联网；
- 确定 UI 不直接修改规则状态；
- 确定确定性随机和事件记录方向。

**产物**：[游戏需求文档.md](./游戏需求文档.md)

### 0.3 TypeScript 工程脚手架 `[x]`

- 配置 `package.json`、`tsconfig.json`、`vitest.config.ts`；
- 建立 `src/` 模块和测试目录；
- 使用 ESM 和严格 TypeScript 选项。

## 五、M1：单局规则引擎（已完成）

### 1.1 牌模型 `[x]`

**文件**：`src/tiles.ts`

- 定义万/筒/条、红中、白板、发财；
- 生成 120 张牌，每种 4 张；
- 提供类型判断、排序和中文标签。

**验收**：牌库数量、每种牌数量和发财隔离测试通过。

### 1.2 随机和牌墙 `[x]`

**文件**：`src/random.ts`、`src/wall.ts`

- 实现洗牌和可复现种子；
- 支持牌头正常摸牌和牌尾补牌；
- 支持发财递归补牌；
- 补牌耗尽抛出可携带已消耗发财信息的错误。

**验收**：递归发财和耗尽测试通过。

### 1.3 胡牌、听牌和番种 `[x]`

**文件**：`src/rules.ts`

- 使用记忆化搜索判断标准 4 面子 + 1 将；
- 支持碰出的面子和各类杠；
- 实现听牌、碰碰胡、清一色和发财番；
- 实现自摸/点炮资格及报嘴条件。

**验收**：普通牌型、多面听、字牌刻子、番种叠加和报嘴规则测试通过。

### 1.4 单局状态机 `[x]`

**文件**：`src/types.ts`、`src/game.ts`

- 状态阶段：`awaiting-draw`、`awaiting-discard`、`claiming`、`finished`、`drawn`；
- 庄家 14 张、其余玩家 13 张；
- 所有动作先经过 `getLegalActions()` 校验；
- `applyAction()` 复制状态后推进，避免直接污染调用方状态。

**验收**：发牌、摸打、状态切换和非法动作测试通过。

### 1.5 碰杠和报嘴约束 `[x]`

- 三家通碰；
- 起手暗杠可用；
- 杠后必须走补牌阶段；
- 报嘴后不能碰杠、只能摸打，摸到胡牌张必须自摸；
- 未报嘴且达到 3 张发财时可以自摸但不能点炮。

**验收**：报嘴、暗杠、明杠、补杠和无抢杠胡测试通过。

### 1.6 结算和荒庄 `[x]`

- 自摸：三家各付 `1 + 番数`；
- 点炮：点炮者付 `1 + 番数`；
- 补杠即时支付；
- 暗杠/明杠终局支付；
- 荒庄退还补杠分，并作废所有终局杠分；
- 杠后补牌胡记为 `gang-draw`。

**验收**：当前测试覆盖自摸、点炮、叠加番、各类杠分、荒庄退款。

### 1.7 弃牌竞争边界 `[x]`

- 以弃牌者下家为第一顺位；
- 沿逆时针收齐三家响应后决胜；
- 前位 `pass` 后继续看下一位；
- 同一窗口可先报嘴，再选择胡或过。

**验收**：非庄家弃牌竞争和前位过牌测试通过。

### 1.8 M1 测试和构建基线 `[x]`

当前基线：

```text
4 个测试文件
28 个测试用例通过
npm run check 通过
npm run build 通过
```

**收尾提交**：`e02c8d5 test: complete mahjong engine closeout`

## 六、M2：本地牌桌 UI（待开始）

M2 的目标不是先做精美界面，而是让一个人可以在浏览器中完整走完一局。UI 只展示视图并提交动作，不自行判断胡牌或结算。

### 2.1 冻结 UI/引擎视图契约 `[x]`

**目标**：先定义 UI 可以依赖的公开数据，避免 UI 直接绑定内部可变状态。

**工作项**：

- 设计 `GameView`、`PlayerView`、`PublicPlayerView` 类型；
- 决定弃牌区是加入 `GameState`，还是从结构化事件派生；
- 将可见信息和内部信息分开：自己的暗牌可见，对手暗牌不可见；
- 规定按钮状态、当前玩家、剩余牌数、报嘴听口和结算信息的字段；
- 保留从完整 `GameState` 到 UI 视图的单向转换。

**验收标准**：

- 有类型定义和至少一组视图转换/弃牌状态测试；
- UI 不需要读取其他玩家的 `concealedTiles`；
- 可以从视图渲染一局的静态牌桌数据。

**完成结果**：

- `PlayerState` 增加显式 `discards`，弃牌被碰/杠/胡后从当前弃牌区移除；
- 新增 `PlayerView` / `PublicPlayerView` / `GameView` 契约类型；
- 新增 `toPlayerView()` / `toPublicPlayerView()`，返回独立副本；
- 新增视图契约和弃牌状态测试。

**提交**：`5688918 feat: define mahjong UI view contract`

### 2.2 实现状态投影 `[x]`

**目标**：实现 `projectStateForSeat(state, seat)`，即使单机版也采用与未来联网相同的隐私边界。

**工作项**：

- 隐藏其他玩家暗牌和牌墙具体顺序；
- 保留对手手牌数量、明牌、发财数量、报嘴状态和弃牌；
- 保留当前玩家可执行动作和当前响应窗口；
- 增加投影不可变性测试，防止调用方修改内部状态。

**验收标准**：四个座位分别投影时，不泄露其他玩家暗牌；事件和分数信息一致。

**完成结果**：

- 实现 `projectStateForSeat(state, seat)`；
- 对手暗手、私有听口、他人摸牌和弃牌响应动作均已裁剪；
- 事件消息按查看座位脱敏；
- 投影数组、事件、支付、结果和动作均为独立副本；
- 新增四个座位、响应窗口和不可变性测试。

**提交**：`55a7201 feat: project privacy-safe game views`

### 2.3 建立浏览器应用壳 `[x]`

**工作项**：

- 引入 React/Vite（或明确选定的等价方案）；
- 增加浏览器入口、开发脚本和生产构建脚本；
- 建立 `App`、牌桌、玩家区、动作区、事件提示等组件边界；
- 接入一个固定种子的演示局。

**验收标准**：浏览器能启动，能显示演示局，不把规则逻辑复制到组件内。

**完成结果**：

- 冻结 React 19 + Vite 8 + TypeScript；
- 规则引擎保持在 `src/`，页面代码放在 `web/`；
- 建立 `App`、牌桌、玩家座位、动作栏和事件流组件；
- 接入固定种子演示局；
- 使用自定义 CSS 完成桌面和 390px 窄屏布局；
- 暂不引入组件库，待通用交互需求明确后再评估 Radix/shadcn。

**提交**：`b66af2a feat: build initial mahjong table shell`

### 2.4 本地回合驱动和占位对手 `[x]`

**工作项**：

- 建立 UI 到 `applyAction()` 的适配层；
- 人类玩家动作由按钮/牌面点击提交；
- 三个对手先使用“永远合法”的简单策略或手动模拟器；
- 处理弃牌响应窗口的等待和自动 `pass`。

**验收标准**：可以从发牌走到胡牌或荒庄，不出现卡死状态。

**完成结果**：

- `ActionBar` 已接入 `applyAction()`，人类座位可以提交所有当前合法动作；
- 基础 Bot 只从 `getLegalActions()` 中选择动作；
- 三个 Bot 会按延迟自动完成摸牌、出牌和弃牌响应；
- 增加 Bot 单元测试和有限步完整对局模拟，验证不会出现无动作中间态；
- 浏览器冒烟验证了人类出牌、Bot 响应、过牌和下一轮摸牌。

**提交**：`a6d7724 feat: connect local play actions and basic bots`

### 2.5 牌桌和操作体验 `[x]`

**工作项**：

- 显示自己的暗牌、发财区、碰杠区；
- 显示对手座位、手牌数量、明牌和报嘴状态；
- 显示四家弃牌区、牌墙剩余数、当前行动者；
- 提供摸牌、出牌、胡、碰、杠、报嘴、过等动作；
- 对报嘴锁定和必须自摸提供清晰提示。

**验收标准**：桌面宽度下操作完整，非法动作不会出现在可点击控件中。

**完成结果**：

- 自己的合法弃牌改为可点击牌面，摸入牌有独立高亮；
- 当前行动、待响应、已响应和已出牌座位有明确状态；
- 动作栏隐藏重复的逐张弃牌按钮，并按胡/碰杠/报嘴/过区分视觉；
- 增加上下文操作提示和最新事件提示；
- 增加胡牌/荒庄结果面板，展示番数、得分和支付流水；
- 修复牌桌因点击焦点产生内部滚动偏移的问题；
- 桌面和 390px 窄屏实际回归通过。

**提交**：`de5ce6c feat: refine mahjong table interaction experience`

### 2.6 事件、结算和回放展示 `[x]`

- 将 `GameEvent` 转成时间线/提示；
- 展示番数明细、支付流水和荒庄退款；
- 支持按事件逐步查看本局；
- 处理刷新或重新进入页面后的本地快照（如纳入 M2）。

**完成结果**：

- 增加完整事件抽屉，支持全部、摸打、碰杠/报嘴和结算筛选；
- 事件和动作使用“你/下家/对家/上家”视角化称呼，并修正牌桌方位标签；
- 从固定种子和动作轨迹重建任意步骤，回放不修改实时牌局；
- 回放时暂停 Bot 和用户动作，可上一步、下一步或返回实时；
- 动作轨迹记录用户、Bot 和自动过牌来源；
- 使用版本化 `localStorage` 保存动作轨迹，刷新后自动恢复；损坏存档会安全回退到新局；
- 采纳问题记录建议：我方唯一合法响应只有“过”时自动过牌，有碰/杠/胡/报嘴选择时仍等待用户；
- 桌面和 390px 抽屉、筛选、回放、恢复流程实际回归通过。

**提交**：`60ce071 feat: persist and replay local game sessions`、`49af06d feat: add round history drawer and replay controls`

### 2.7 M2 验收 `[x]`

必须完成以下完整演练：

- 普通自摸；
- 点炮和点炮拒绝；
- 发财递归补牌；
- 碰、明杠、暗杠、补杠；
- 报嘴后摸打和杠上开花；
- 荒庄以及补杠分退款。

**完成结果**：

- 新增 `npm run test:acceptance`，固化 6 个可重复验收场景；
- 覆盖普通自摸、点炮与拒炮、发财递归、三类杠、报嘴、杠上开花和荒庄退款；
- 使用内置 SVG 矢量图实现万、筒、条、一条鸟形、红中和白板，无外部图片依赖；
- 牌面支持手牌、弃牌和响应式缩放，并保留无障碍名称；
- 桌面和 390px 视觉/交互检查无溢出、无控制台错误；
- M2 退出标准已满足。

**提交**：`46ba07d feat: render authentic vector mahjong tiles`、`9f28bcd test: add repeatable M2 acceptance scenarios`

## 七、M3：AI 对手（待开始）

M3 先追求稳定、可解释和可复现，不直接引入机器学习。

### 3.1 合法动作代理 `[x]`

- AI 只能从 `getLegalActions()` 结果中选择动作；
- 为摸牌、出牌、响应、报嘴和杠建立统一接口；
- 发现异常状态时安全 `pass` 或结束当前决策，不破坏局面。

**完成结果**：

- 新增独立于 React 的 `AiPolicy` / `AiDecision` / `decideAiAction()` 接口；
- 决策包含动作、原因、阶段、策略 ID、合法动作数量和回退来源；
- 所有策略输出均与 `getLegalActions()` 比对；
- 策略抛错、返回空值或非法动作时，按阶段安全回退到过牌、摸牌或弃牌；
- 原有本地 Bot 已切换到统一 AI 代理。

**提交**：`3197325 feat: add legality-safe AI decision interface`

### 3.2 手牌评估 `[x]`

- 计算向听/有效进张的基础指标；
- 估计碰碰胡、清一色和发财番的潜在收益；
- 将明牌、报嘴和剩余牌信息纳入评估；
- 与 `rules.ts` 共用牌型判断，不另写胡牌算法。

**完成结果**：

- 实现标准 4 面子 + 1 将的精确向听数（成胡 -1、听牌 0）；
- 计算有效进张及基于自己暗手、所有公开弃牌/明牌的可见剩余张数；
- 评估清一色目标花色、碰碰胡结构和发财番潜力；
- 为每个合法弃牌生成排序评分与文字原因；
- 测试确认改变对手暗手不会影响 AI 估值，避免偷看私密信息。

**提交**：`d038499 feat: evaluate shanten and discard potential`

### 3.3 出牌策略 `[x]`

- 优先保留有效搭子；
- 评估换听成本和潜在番数；
- 报嘴后严格执行摸到什么打什么；
- 记录决策原因，便于调试和回放。

**完成结果**：

- AI 按向听、有效进张和番种潜力选择最高分合法弃牌；
- 增加孤张字牌、孤立幺九、对子/刻子保留及清一色目标花色规则；
- 报嘴后仍严格使用规则引擎提供的唯一摸打动作；
- `AiDecision` 记录候选动作、评分和原因；
- Bot 决策说明与前三个备选评分写入动作轨迹，可在回放抽屉查看。

**提交**：`c80faa2 feat: add explainable strategic discard AI`

### 3.4 碰杠和报嘴策略 `[x]`

- 定义碰、明杠、暗杠、补杠的收益阈值；
- 定义 ≥3 张发财且听牌时的自动报嘴策略；
- 定义未报嘴时自摸胡/放弃胡的策略；
- 不违反定稿中的点炮和报嘴限制。

**完成结果**：

- 合法自摸/点炮默认接受，规则强制场景不会拒绝；
- 三财以上且听牌时自动报嘴，随后按锁定听口行动；
- 碰牌会比较前后向听、进张与碰碰胡结构，效率明显下降时过牌；
- 明杠比较前后手牌效率及墙尾可补牌条件；
- 暗杠仅在不增加向听时采用，补杠因即时得分和补牌机会优先；
- 所有选择继续通过统一合法动作代理并写入解释日志。

**提交**：`d5b4ad7 feat: add strategic claim and kong decisions`

### 3.5 难度、随机性和测试 `[x]`

- 通过参数控制保守/均衡/激进策略；
- 使用固定随机种子重放 AI 决策；
- 测试 AI 不执行非法动作；
- 运行大量短局，检查不会出现死循环或状态泄漏。

**完成结果**：

- 增加休闲、标准、进阶三档 AI，并在页面提供实时切换；
- 休闲档可选择任意评分候选，标准档在近优候选中选择，进阶档始终选择最高分；
- 碰杠/报嘴的可选动作按难度使用不同接受率，胡牌始终遵守规则；
- 随机源由种子和动作序号确定，刷新与回放可复现；
- 新增全 AI 整局模拟器，检查完成状态、最大动作数和零和计分；
- 新增 `npm run simulate:ai -- --games=N --seed=N --difficulty=...` 统计报告；
- 页面回放可显示 AI 选择原因与前三个备选评分。

**提交**：`4c5669c feat: add deterministic AI difficulty simulation`

### 3.6 M3 验收 `[x]`

- 三个 AI 能完整打完指定数量的局；
- 每局事件日志可重放；
- AI 的动作全部通过规则引擎校验；
- 基础策略和困难策略在统计上有可区分行为。

**完成结果**：

- 休闲、标准、进阶各运行 5 局独立种子全 AI 对局，15/15 全部完成；
- 所有对局在动作上限内结束且最终四家分数和为 0；
- 三档最优弃牌率分别为 11.28%、91.79%、100%，行为可区分；
- 相同种子和难度的决策、整局摘要均可复现；
- 页面难度切换与回放决策解释无运行时错误；
- 详细结果见 [AI验收报告.md](./AI验收报告.md)。

**验收提交**：`4c5669c feat: add deterministic AI difficulty simulation`

## 八、M4：多局与联网（待开始）

M4 必须建立在 M2 的状态投影和 M3 的动作适配层之上，不能直接把完整 `GameState` 广播给客户端。

### 4.1 多局会话层 `[x]`

- 建立 `Match`/`Session`，管理多局；
- 记录累计分和本局结果；
- 明确上局赢家、首局庄家和下一局开局规则；
- 处理荒庄后的庄家和继续条件；
- 明确骰子与开门位置映射。

**前置决策**：骰子规则、局数、结束条件。

**完成结果**：

- 新增 `MatchState`，管理局号、阶段、当前庄家、累计分和历史结果；
- 一场默认 8 局，可配置局数和荒庄留庄/轮庄策略；
- 首局四家掷两骰比大小，平点者确定性重掷；也支持测试/房间指定首庄；
- 每边按 15 墩、每墩 2 张将骰子映射到开门墙和线性牌墙偏移；
- 有人胡时赢家成为下一局庄家，荒庄默认原庄保留；
- 每局使用独立派生种子，牌墙、骰子和开门位置可复现；
- 单局结束自动累计零和分数，局间禁止继续提交旧局动作。

**提交**：`702544c feat: add deterministic multi-round match sessions`

### 4.2 服务端权威引擎 `[x]`

- 服务端持有完整状态和随机源；
- 客户端只能提交动作意图，不能提交牌墙/手牌/分数；
- 服务端验证座位、阶段、动作和响应时序；
- 记录可审计事件日志和快照。

**完成结果**：

- 新增 `AuthoritativeRoom`，完整 `MatchState` 仅保存在房间内部；
- 客户端动作携带请求 ID、预期修订号、座位与动作意图；
- 服务端校验修订号、动作座位、比赛阶段及合法动作集；
- 相同请求可幂等重试，同 ID 不同命令会被拒绝；
- 过期、冒用座位和非法动作均返回结构化错误与最新座位快照；
- 每次响应只包含该座位的隐私投影，牌墙和对手暗手不外泄；
- 审计日志记录动作类型、修订号变化和拒绝原因。

**提交**：`203f3fb feat: add authoritative room action service`

### 4.3 网络协议和状态投影 `[x]`

- 定义连接、加入房间、动作、事件、快照和错误消息；
- 每个座位只收到自己的私密视图；
- 处理 claiming 阶段的三方响应和超时；
- 协议版本化，避免 UI 和服务器隐式耦合。

**完成结果**：

- 定义协议 v1 的 hello、加入、快照、动作、下一局、事件、错误和 ping/pong 消息；
- `parseClientMessage()` 对 JSON、版本、请求 ID、修订号、动作和字段长度做运行时校验；
- 客户端动作意图不含可信座位，即使附带座位字段也会被解析器丢弃；
- `handleRoomProtocolMessage()` 使用连接上下文注入座位并调用权威房间；
- 动作成功返回结果和座位可见事件批次，错误返回结构化响应；
- 四座位协议快照测试确认每份视图只有一个私有玩家；
- 详细契约见 [联网协议.md](./联网协议.md)。

**提交**：`61dd1a4 feat: define versioned multiplayer protocol`

### 4.4 房间和生命周期 `[x]`

- 创建/加入/离开房间；
- 房间密码、座位占用和观战权限；
- 房主/服务器关闭时的状态保存；
- 断线玩家的席位保留和重新接入。

**完成结果**：

- 新增 `RoomRegistry` 与 `MultiplayerGateway`，支持创建、列出、加入和主动离开；
- 四个玩家座位支持偏好选择、占用检查和离开释放；
- 创建者成为房主，离开后按加入顺序自动转移房主；
- 房间密码只保存随机盐 + SHA-256 摘要，并以常量时间比较；
- 参与者获得不可猜测的恢复令牌，公开大厅不包含令牌或密码摘要；
- 观战者不占玩家座位，有独立上限，收到全公开、零合法动作的观战投影；
- 空房在默认 5 分钟保留期后可由注册表回收；
- 协议网关将连接 ID 绑定参与者身份，并拒绝观战者动作。

**提交**：`5f30843 feat: add secure room lifecycle registry`

### 4.5 断线重连和托管 `[x]`

- 使用快照 + 事件序列恢复；
- 重连后只补发该座位可见的信息；
- 定义出牌超时和响应超时的默认动作；
- 托管 AI 不得获得额外私密信息。

**完成结果**：

- 区分主动离开和网络断线：断线保留参与者、座位与恢复能力；
- 恢复令牌绑定原身份，新连接恢复后立即轮换令牌，防止重复使用；
- 恢复响应包含当前修订号和对应座位隐私快照/事件序列；
- 玩家断线后立即进入托管，也支持在线玩家主动开关托管；
- 默认出牌 30 秒、响应 12 秒；响应超时优先 `pass`，其他超时由同座 AI 处理；
- 托管动作仍通过合法动作代理、权威修订号、幂等和审计；
- 默认断线保留 2 分钟，过期后释放座位并按需转移房主。

**提交**：`9d61d0a feat: add reconnect timeout and trustee flows`

### 4.6 安全和反作弊 `[x]`

- 不信任客户端提交的牌、番数和支付；
- 防止重复动作、重放请求和跨房间座位伪造；
- 服务器记录关键状态转移；
- 对异常频率和非法动作进行限流/封禁策略评估。

**完成结果**：

- 默认拒绝超过 16 KiB 的消息，并限制 10 秒内最多 60 个请求；
- 重复非法消息、错误认证、非法动作、身份冲突和限流进入违规计数；
- 默认累计 5 次违规后临时封禁 60 秒，过期自动恢复；
- 请求 ID 幂等、修订号和连接身份共同防止重放、旧动作和跨座位伪造；
- 审计只保存连接、时间、类别和短详情，不保存密码、令牌或消息体；
- 增加错误密码、令牌/消息泄漏、限流和封禁恢复测试；
- 详细威胁与部署责任见 [联网安全设计.md](./联网安全设计.md)。

**提交**：`e526ead feat: add multiplayer abuse protection`

### 4.7 M4 验收 `[x]`

- 四个客户端可完成至少一局；
- 客户端之间不泄露暗牌；
- 并发响应结果稳定且顺序正确；
- 断线重连后可以继续；
- 服务端重启或恢复策略经过演练。

**完成结果**：

- 四个独立协议连接和一个观战连接完成整局，逐步验证隐私投影；
- 并发同修订响应稳定表现为一条接受、一条过期，更新后继续；
- 演练断线令牌恢复、托管和协议开始下一局；
- 权威房间、幂等缓存、审计、参与者和令牌支持服务端持久化恢复；
- 新增真实 HTTP + WebSocket 服务、逐连接隐私广播和 `0600` 原子状态文件；
- 浏览器端提供创建/加入/观战、在线牌桌、托管、离开和自动恢复；
- 详细结果见 [联网验收报告.md](./联网验收报告.md)。

**提交**：`2e4bba2 feat: persist rooms and add multiplayer acceptance flow`、`a7cb91a feat: add persistent WebSocket game server`、`303672c feat: add browser multiplayer mode`

## 九、M5：质量与发布（待开始）

### 5.1 自动化质量 `[x]`

- 单元测试、状态机测试、AI 测试和 UI E2E 测试分层；
- CI 执行 `npm ci`、`npm run check`、`npm test`、`npm run build`；
- 对随机局和固定种子局做回归。

**完成结果**：

- 新增 GitHub Actions（Node 22 + `npm ci --ignore-scripts` + `npm run quality`）；
- 新增 Prettier 写入/检查和受版本控制文件卫生检查；
- 卫生检查拒绝构建产物、本地数据、密钥、大文件和合并冲突标记；
- 测试分为 `test:fast`、`test:integration`、`test:acceptance` 和全量覆盖率；
- 覆盖率门槛：statements/lines 75%、functions 70%、branches 65%；
- 当前总体 statements 76.57%、branches 69.11%、functions 75.72%、lines 79.63%；
- `npm run quality` 串联格式、卫生、三套 TypeScript 检查、覆盖率、验收和三类构建。

**提交**：`803186d style: format TypeScript and web sources`、`7327605 ci: add coverage formatting and quality gates`

### 5.2 体验和兼容性 `[x]`

- 桌面浏览器和手机横屏布局；
- 键盘/触控可操作性；
- 动画不改变规则状态；
- 事件提示、结算和错误信息可理解。

**完成结果**：

- 桌面、390×844 竖屏和 844×390 横屏均无页面横向溢出；
- 增加跳过链接、主内容锚点、语义化模式按钮和统一键盘焦点；
- 记录抽屉打开时聚焦关闭按钮，关闭后恢复触发按钮；
- 移动端主要按钮最小 44px，手牌支持触控横向滚动；
- 支持 `prefers-reduced-motion` 和系统强制颜色模式；
- 增加不泄露牌局/令牌的 React 错误边界；
- 实测 WebSocket 停止时显示离线，服务恢复后自动重连；
- 详细结果见 [兼容性验收报告.md](./兼容性验收报告.md)。

**提交**：`a3daf03 feat: improve accessibility and responsive resilience`

### 5.3 发布 `[x]`

- 固定版本号和变更日志；
- 完善运行、部署、回滚和故障排查文档；
- 明确单机版/联网版的数据和隐私边界。

**完成结果**：

- 固定首个版本为 v1.0.0，完善包名、入口、类型、Node 版本和变更日志；
- 增加 `/healthz`、`/readyz`、CSP 与生产安全响应头；
- 增加 Node 22 多阶段 Dockerfile、Compose、非 root 运行和持久卷；
- 增加环境变量示例、ISC 许可证和完整部署/备份/回滚文档；
- 使用锁文件完成干净安装并再次通过 `npm run quality`；
- 原生生产服务、HTTP、健康端点和 WebSocket 测试通过；
- `docker compose config` 通过；Docker daemon 未运行，镜像实际构建留给启用 Docker 的 CI/部署机；
- npm 官方生产依赖审计报告 0 个漏洞；
- 详细结果见 [发布验收报告.md](./发布验收报告.md)。

**提交**：`631b009 release: prepare v1.0.0 deployment`

## 十、决策台账

| 编号 | 决策 | 状态 | 当前约定/待定内容 |
|---|---|---:|---|
| D-01 | 弃牌竞争顺序 | [x] | 弃牌者下家起，沿逆时针处理 |
| D-02 | 前位 `pass` 后的响应 | [x] | 继续看下一位非 `pass` 响应 |
| D-03 | 响应窗口内报嘴 | [x] | 可以先报嘴，再选择胡或过 |
| D-04 | 骰子到座位/开门位置映射 | [x] | 两骰总点映射墙方位；每边 15 墩、每墩 2 张计算牌墙偏移 |
| D-05 | 多局结束与下一局庄家 | [x] | 默认 8 局；赢家成为下一庄，荒庄默认留庄，可配置轮庄 |
| D-06 | 弃牌数据的内部表示 | [x] | `PlayerState.discards` 保存当前未被鸣牌/胡走的弃牌；事件保留完整历史 |
| D-07 | 前端技术方案 | [x] | React 19 + Vite 8 + TypeScript；牌桌使用自定义 CSS，通用组件库延后评估 |
| D-08 | 超时默认策略 | [x] | 默认出牌 30 秒、响应 12 秒；响应超时按 `pass`，其他超时或断线由同座 AI 托管 |
| D-09 | 仅有“过”时是否手动确认 | [x] | 本地版自动过牌；存在其他合法响应时不自动处理 |
| D-10 | 逼真麻将牌面资源 | [x] | 使用代码内置 SVG 矢量牌面，避免外部图片依赖并支持无损缩放 |

## 十一、当前下一步

当前实施计划内所有已知任务均已完成。

后续新增功能应先在本台账增加新的里程碑或步骤，再按“实现 → 验证 → 更新台账 → 本地 commit”的流程执行。

当前仓库不自动 push，也不自动创建远端 Release；发布动作由仓库所有者决定。

## 十二、进度日志

| 日期 | 步骤 | 结果 | Commit |
|---|---|---|---|
| 2026-09-04 | M0 规则与工程基线 | 完成规则定稿、需求文档和 TypeScript 脚手架 | `9c5ec0e` / `bd9cf76` |
| 2026-09-04 | M1 规则引擎主体 | 完成牌墙、牌型、状态机、碰杠、报嘴和结算 | `bd9cf76` |
| 2026-09-04 | M1 收尾 | 补充结算和边界测试；统一规则/文档；28 个测试通过 | `e02c8d5` |
| 2026-09-04 | M2.1 视图契约 | 增加弃牌区字段、公开/自身玩家视图类型和契约测试；30 个测试通过 | `5688918` |
| 2026-09-04 | M2.2 状态投影 | 实现按座位的隐私投影、事件脱敏和深拷贝；32 个测试通过 | `55a7201` |
| 2026-09-04 | M2.3 浏览器应用壳 | 冻结 React/Vite 方案，完成牌桌静态演示、桌面/窄屏布局和浏览器检查 | `b66af2a` |
| 2026-09-04 | M2.4 本地回合驱动 | 接入人类动作、三个基础 Bot、弃牌响应自动处理和有限步完整对局模拟；38 个测试通过 | `a6d7724` |
| 2026-09-05 | M2.5 操作体验 | 完成牌面点击、状态高亮、操作提示和结算面板；43 个测试及桌面/窄屏回归通过 | `de5ce6c` |
| 2026-09-05 | M2.6 事件与回放 | 完成完整事件筛选、动作轨迹、逐步回放、自动存档恢复和仅过自动响应；49 个测试通过 | `60ce071` / `49af06d` |
| 2026-09-05 | M2.7 总体验收 | 完成 SVG 逼真牌面、6 个验收场景及桌面/窄屏回归；59 个测试通过，M2 完成 | `46ba07d` / `9f28bcd` |
| 2026-09-05 | M3.1 合法动作代理 | 建立统一可解释 AI 决策接口与非法/异常安全回退；63 个测试通过 | `3197325` |
| 2026-09-05 | M3.2 手牌评估 | 完成向听、有效进张、公开剩余牌、番种潜力和弃牌解释评分；67 个测试通过 | `d038499` |
| 2026-09-05 | M3.3 出牌策略 | 完成向听/进张驱动的弃牌选择、结构启发式和回放决策解释；71 个测试通过 | `c80faa2` |
| 2026-09-05 | M3.4 碰杠与报嘴 | 完成胡牌、报嘴、碰牌效率比较和三类杠收益判断；75 个测试通过 | `d5b4ad7` |
| 2026-09-05 | M3.5 难度与模拟 | 完成三档确定性 AI、页面切换、整局模拟和统计报告；81 个测试通过 | `4c5669c` |
| 2026-09-05 | M3.6 AI 总验收 | 三档各 5 局全部完成、零和计分、行为差异明确，M3 完成 | `4c5669c` |
| 2026-09-05 | M4.1 多局会话 | 完成骰子开门、多局累计、下一庄、局间阶段和可配置结束条件；86 个测试通过 | `702544c` |
| 2026-09-05 | M4.2 权威房间 | 完成服务端状态持有、隐私快照、修订号、幂等和审计；91 个测试通过 | `203f3fb` |
| 2026-09-05 | M4.3 联网协议 | 完成协议 v1、运行时校验、连接座位注入、事件消息和四座位隐私测试；98 个测试通过 | `61dd1a4` |
| 2026-09-05 | M4.4 房间生命周期 | 完成密码、房主、座位、观战、安全令牌、连接网关和空房回收；108 个测试通过 | `5f30843` |
| 2026-09-05 | M4.5 重连与托管 | 完成断线保座、令牌轮换恢复、主动/断线托管和阶段超时；114 个测试通过 | `9d61d0a` |
| 2026-09-05 | M4.6 安全与反作弊 | 完成消息大小/频率限制、违规封禁、身份/令牌防护和脱敏审计；119 个测试通过 | `e526ead` |
| 2026-09-05 | M4.7 联网总验收 | 四连接整局、并发、观战、重连、真实 WebSocket、浏览器和重启恢复通过，M4 完成 | `2e4bba2` / `a7cb91a` / `303672c` |
| 2026-09-05 | M5.1 自动化质量 | 完成 CI、Prettier、仓库卫生、测试分层和覆盖率门槛；`npm run quality` 通过 | `803186d` / `7327605` |
| 2026-09-05 | M5.2 体验与兼容性 | 完成三类视口、键盘焦点、触控、减弱动画、错误边界和离线恢复验收 | `a3daf03` |
| 2026-09-05 | M5.3 v1.0.0 发布准备 | 完成版本、变更日志、健康检查、容器、部署/回滚和干净生产验收，M0-M5 全部完成 | `631b009` |

以后每完成一个步骤，都在这里追加一行，并同步更新本文档顶部的“当前阶段”和对应状态标记。
